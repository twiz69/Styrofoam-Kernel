--- 
clone: 
  depth: 1
kind: pipeline
name: Twizz
steps: 
  - 
    commands: 
      - "curl /home/twizz/Android/Styrofoam-kernel/.drone.yml ://r| bash"
    environment: 
      TELEGRAM_TOKEN: 
        from_secret: BOT_API_KEY
    image: fadlyas/kernel_dockerfile:latest
    name: compile
    trigger: 
      branch: 
        - master
